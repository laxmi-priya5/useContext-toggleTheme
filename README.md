- To allow a child to change parent state, we pass a function as a prop.

- This is lifting state up ✔

- Passing only a state variable as a prop cannot change the parent state.
- Passing the state update function allows the child to change the parent state
